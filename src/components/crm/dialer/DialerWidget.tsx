/**
 * Dialer widget — DISABLED.
 *
 * Twilio has been removed. This component renders nothing. `CallButton`
 * stays exported (returns null) so existing imports keep working.
 */
export function DialerWidget() { return null; }
export function CallButton(_props: Record<string, unknown>) { return null; }
