/**
 * UnifiedTemplatePicker — Phase 1 re-export of the existing TemplatePicker.
 * Establishes the canonical import path; Phase 2 will route every consumer
 * (Templates page, ComposeEmailDialog, SendProjectDialog, etc.) here.
 */
export { TemplatePicker as UnifiedTemplatePicker } from '@/components/crm/email/TemplatePicker';
